# SQLite migration archive

These migrations were used when the app targeted SQLite (`provider = "sqlite"`).

Production on Railway uses **PostgreSQL**. The active migration history is in
`../migrations/` (Postgres). Keep this folder only as a historical reference /
for recovering an old local SQLite database.

Do not run these against Postgres.
